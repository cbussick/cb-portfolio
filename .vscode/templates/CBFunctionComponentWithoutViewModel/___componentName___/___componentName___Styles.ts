import { createStyles, makeStyles, Theme } from "@material-ui/core";

export const use___componentName___Styles = makeStyles((theme: Theme) =>
  createStyles({
    wrapper: {
      backgroundColor: theme.palette.primary.main,
    },
  })
);
